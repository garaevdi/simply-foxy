project(
  'simplyfoxy',
  'cpp',
  version: '0.1.0',
  license: 'GPL-3.0-or-later',
  meson_version: '>=0.59'
)

app_id = 'io.github.garaevdi.' + meson.project_name()
app_path = '/' + app_id.replace('.', '/')

subdir('data')
subdir('src')
